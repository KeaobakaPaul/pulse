import React from "react";
import ReactDOM from "react-dom/client";
import PulseSocial from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <PulseSocial />
  </React.StrictMode>
);
